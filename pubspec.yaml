name: quiz_oiseaux
description: Quiz d'identification d'oiseaux
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.1.0
  confetti: ^0.8.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/oiseaux.json
    - assets/images/
    - assets/sounds/correct.mp3
    - assets/sounds/wrong.mp3